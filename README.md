## Intro


- git clone
    ```bash
        git clone https://github.com/TsPuujee/docker-mysql-php-example
    ```
- start docker
    ```bash
        docker-compose up -d
    ```

- stop docker
    ```bash
        docker-compose down
    ```